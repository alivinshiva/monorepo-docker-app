# Copy this file to .env and update DATABASE_URL with your database credentials.
# Do NOT commit real credentials to version control.

# PostgreSQL example (recommended if your schema uses postgresql):
# DATABASE_URL="postgres://username:password@localhost:5432/my_database"

# SQLite example (for quick local testing only).
# NOTE: If you use SQLite, update `provider = "postgresql"` to `provider = "sqlite"` in prisma/schema.prisma.
# DATABASE_URL="file:./dev.db"
